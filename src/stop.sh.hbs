#! {{shell}}

{{#if (has_session name)}}
    # TODO Include default if root is not set?
    cd {{root}}

    # Run on_project_stop command
    {{#if hook_on_project_stop}}
	  {{{hook_on_project_stop}}}
 	{{/if}}

    {{{tmux_command}}} kill-session -t {{name}}
{{/if}}
